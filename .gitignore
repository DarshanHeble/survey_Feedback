# Environment config & secrets
# env.js is intentionally tracked - it's required by the browser at runtime

# Node / dependencies
node_modules/
.env
.env.local
.env.*.local

# OS / Editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
